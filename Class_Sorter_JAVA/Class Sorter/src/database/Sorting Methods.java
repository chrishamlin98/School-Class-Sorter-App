package database

class Sorting Methods{

  public class ReverseSort {
    for (int i = 0; i < array.length / 2; i++) {
            array b1 = array[i];
            array[i] = array[array.length - 1 - i];
            array[array.length - 1 - i] = b1;
    }
  }
}
